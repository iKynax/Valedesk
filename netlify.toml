[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA routing — all non-API/non-asset paths serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
